'use client';

import ChatPage from '@/app/(app)/(convo)/convo/[id]/page';

export default function NewChatPage() {
	// TODO: Show the ConvoID Screen and replace the path
	// return (
	// 	<div className="relative flex h-full flex-1 flex-col">
	// 		<UserInput />
	// 	</div>
	// );
	return <ChatPage />;
}
